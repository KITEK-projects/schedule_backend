services:
  db:
    container_name: db
    image: postgres
    restart: no
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: a_yYIY%4M7aGbl!
    volumes:
      - ./db:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  # web:
  #   container_name: web
  #   image: nginx
  #   restart: always
  #   volumes:
  #     - ./web/kitek/dist/:/usr/share/nginx/html:ro
  #   ports:
  #     - "8080:80"
  #   environment:
  #     - NGINX_PORT=80

  api:
    container_name: api
    build: api/
    restart: always
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - /home/xaori/schedule_backend/api/media/:/app/media
      - /home/xaori/schedule_backend/api/static/:/app/static
